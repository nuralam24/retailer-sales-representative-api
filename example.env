PORT=9999
NODE_ENV=dev

# Database Configuration (Prisma)
# Option 1: Use LOCAL database (for development)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/retailer_sales_representative_app?schema=public"

# Option 2: Use DOCKER database (for full Docker setup)
# DATABASE_URL="postgresql://postgres:postgres@postgres:5432/retailer_sales_representative_app?schema=public"

# Note: d-quick-run.sh will automatically detect which database you're using
# - If using 'localhost' → Uses your local PostgreSQL (existing data preserved)
# - If using 'postgres' → Creates new Docker database and seeds automatically

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_TTL=3600

# JWT Configuration (Change these in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-chars
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
# Comma-separated list of allowed origins (use * for development only)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Logging Configuration
LOG_PATH=logs
LOG_ERROR=error.log
LOG_ACCESS=access.log

# Show Nest startup logs (true/false)
SHOW_NEST_LOGS=false